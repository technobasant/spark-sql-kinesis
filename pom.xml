<?xml version="1.0" encoding="UTF-8"?>
<!--
~ Licensed to the Apache Software Foundation (ASF) under one or more
~ contributor license agreements.  See the NOTICE file distributed with
~ this work for additional information regarding copyright ownership.
~ The ASF licenses this file to You under the Apache License, Version 2.0
~ (the "License"); you may not use this file except in compliance with
~ the License.  You may obtain a copy of the License at
~
~    http://www.apache.org/licenses/LICENSE-2.0
~
~ Unless required by applicable law or agreed to in writing, software
~ distributed under the License is distributed on an "AS IS" BASIS,
~ WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
~ See the License for the specific language governing permissions and
~ limitations under the License.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.roncemer.spark</groupId>
  <artifactId>spark-sql-kinesis_2.13</artifactId>
  <version>1.3.0_spark-4.2.0</version>
  <packaging>jar</packaging>
  <name>Kinesis Integration for Structured Streaming</name>
  <description>Connector to read from and write into Kinesis from Structured Streaming Applications</description>
  <url>http://github.com/roncemer/spark-sql-kinesis</url>


  <developers>
    <developer>
      <id>technobasant</id>
      <organization>Techno Basant</organization>
      <organizationUrl>https://github.com/technobasant</organizationUrl>
      <roles>
        <role>developer</role>
      </roles>
    </developer>
    <developer>
      <id>roncemer</id>
      <organization>Ron Cemer</organization>
      <organizationUrl>https://roncemer.com</organizationUrl>
      <roles>
        <role>developer</role>
      </roles>
    </developer>
    <developer>
      <id>qubole</id>
      <organization>Qubole Inc.</organization>
      <organizationUrl>http://www.qubole.com</organizationUrl>
      <roles>
        <role>developer</role>
      </roles>
    </developer>
  </developers>

  <licenses>
    <license>
      <name>Apache License, Version 2.0</name>
      <url>https://github.com/roncemer/spark-sql-kinesis/blob/master/LICENSE</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <scm>
    <connection>scm:git:git://github.com/roncemer/spark-sql-kinesis.git</connection>
    <url>http://github.com/roncemer/spark-sql-kinesis</url>
    <developerConnection>scm:git:git@github.com:roncemer/spark-sql-kinesis.git</developerConnection>
    <tag>spark-sql-kinesis_2.13-1.3.0_spark-4.2.0</tag>
  </scm>

  <inceptionYear>2018</inceptionYear>
  <organization>
    <name>Qubole</name>
    <url>http://www.qubole.com/</url>
  </organization>

  <properties>
    <sbt.project.name>sql-kinesis</sbt.project.name>
    <spark.version>4.2.0</spark.version>
    <scala.binary.version>2.13</scala.binary.version>
    <!--
      Matches spark-parent_2.13:4.2.0's own scala.version, which is also the scala-library
      that spark-sql pulls in transitively and that the Spark dist ships. Keep compiler and
      library equal: a compiler older than scala-library trips SIP-51, and a newer one
      compiles against an older library. Bump only together with Spark.
    -->
    <scala.version>2.13.18</scala.version>
    <java.version>17</java.version>
    <!--
      Held at the version spark-parent_2.13:4.2.0 pins, deliberately not the newest jackson.
      jackson-core/annotations/databind are not shaded into our jar, so at runtime Spark's
      copy wins; declaring a different version here would only create a compile/runtime skew.
    -->
    <fasterxml.jackson.version>2.21.2</fasterxml.jackson.version>
    <!--
      AWS SDK v2 replaces v1 (EOL 2025-12-31). Both are current stable releases, ahead of
      the 2.44.5 / 1.0.6 pair the awslabs connector ships.
      The KPL groupId moved from com.amazonaws to software.amazon.kinesis; 1.0.x is the
      SDK-v2-native line and its setCredentialsProvider takes an v2 AwsCredentialsProvider.
    -->
    <aws.sdkv2.version>2.51.3</aws.sdkv2.version>
    <aws.kpl.version>1.0.7</aws.kpl.version>
    <!--
      Bundled into the shaded jar because ApacheHttpClient needs them, so their currency is
      our responsibility rather than the host classpath's. 4.5.14 / 4.4.16 are the final 4.x
      releases; SDK v2's apache-client is built against the 4.x line, so there is no 5.x path
      here without swapping the HTTP client itself.
    -->
    <httpclient.version>4.5.14</httpclient.version>
    <httpcore.version>4.4.16</httpcore.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
  </properties>

  <!--
    Jackson artifacts are not released in lockstep (there is no jackson-annotations
    2.21.2), so individual versions cannot be pinned from one property. Import the BOM
    and let it resolve each artifact, which is what spark-parent does.
  -->
  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>com.fasterxml.jackson</groupId>
        <artifactId>jackson-bom</artifactId>
        <version>${fasterxml.jackson.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>software.amazon.awssdk</groupId>
        <artifactId>bom</artifactId>
        <version>${aws.sdkv2.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.apache.spark</groupId>
      <artifactId>spark-sql_${scala.binary.version}</artifactId>
      <version>${spark.version}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.spark</groupId>
      <artifactId>spark-core_${scala.binary.version}</artifactId>
      <version>${spark.version}</version>
      <type>test-jar</type>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.spark</groupId>
      <artifactId>spark-catalyst_${scala.binary.version}</artifactId>
      <version>${spark.version}</version>
      <type>test-jar</type>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.spark</groupId>
      <artifactId>spark-sql_${scala.binary.version}</artifactId>
      <version>${spark.version}</version>
      <type>test-jar</type>
      <scope>test</scope>
    </dependency>
    <!--
      AWS SDK v2. Versions come from the bom import above.
      KCL 1.x is gone entirely: it existed only for UserRecord.deaggregate, which is now
      handled by the vendored AggregatorUtil against the KPL protobuf format.
    -->
    <dependency>
      <groupId>software.amazon.awssdk</groupId>
      <artifactId>kinesis</artifactId>
    </dependency>
    <dependency>
      <groupId>software.amazon.awssdk</groupId>
      <artifactId>sts</artifactId>
    </dependency>
    <dependency>
      <groupId>software.amazon.awssdk</groupId>
      <artifactId>auth</artifactId>
    </dependency>
    <dependency>
      <groupId>software.amazon.awssdk</groupId>
      <artifactId>regions</artifactId>
    </dependency>
    <dependency>
      <groupId>software.amazon.awssdk</groupId>
      <artifactId>apache-client</artifactId>
    </dependency>
    <dependency>
      <groupId>software.amazon.kinesis</groupId>
      <artifactId>amazon-kinesis-producer</artifactId>
      <version>${aws.kpl.version}</version>
    </dependency>
    <dependency>
      <groupId>com.google.protobuf</groupId>
      <artifactId>protobuf-java</artifactId>
      <!--
        KPL 1.0.7's generated Messages classes are produced by protoc 4.29.0 (its pom
        declares protobuf-java-version 4.29.0). A newer 4.x runtime accepts older 4.x
        gencode, so this runs ahead of both KPL's and the awslabs connector's pin to pick up
        protobuf fixes; AggregatorUtilSuite is what proves the gencode still loads.
        Do not drop below 4.29.0: on 3.21.6 the generated code dies at class-init with
        NoClassDefFoundError: com/google/protobuf/RuntimeVersion$RuntimeDomain, which breaks
        deaggregation at runtime rather than at build time.
      -->
      <version>4.35.1</version>
    </dependency>
    <!-- Explicit so the bundled HTTP stack is pinned by us, not by apache-client's own pom. -->
    <dependency>
      <groupId>org.apache.httpcomponents</groupId>
      <artifactId>httpclient</artifactId>
      <version>${httpclient.version}</version>
    </dependency>
    <dependency>
      <groupId>org.apache.httpcomponents</groupId>
      <artifactId>httpcore</artifactId>
      <version>${httpcore.version}</version>
    </dependency>
    <!-- Versions come from the jackson-bom import above. -->
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-core</artifactId>
    </dependency>
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-annotations</artifactId>
    </dependency>
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
    </dependency>
    <dependency>
      <groupId>com.fasterxml.jackson.dataformat</groupId>
      <artifactId>jackson-dataformat-cbor</artifactId>
    </dependency>
    <!--
      mockito-core and scalacheck were declared here but no test referenced them, so they
      were dropped rather than upgraded.

      scalatest is pinned to the version spark-parent_2.13:4.1.2 pins, NOT the newest
      release (3.2.20). Our suites extend StreamTest / SharedSparkSession / OffsetSuite out
      of Spark's test-jars, which are compiled against 3.2.19; running a different scalatest
      than those classes were built against risks binary incompatibility for no benefit.
    -->
    <dependency>
      <groupId>org.scalatest</groupId>
      <artifactId>scalatest_${scala.binary.version}</artifactId>
      <version>3.2.19</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.spark</groupId>
      <artifactId>spark-tags_${scala.binary.version}</artifactId>
      <version>${spark.version}</version>
    </dependency>

    <!--
      This spark-tags test-dep is needed even though it isn't used in this module, otherwise testing-cmds that exclude
      them will yield errors.
    -->
    <dependency>
      <groupId>org.apache.spark</groupId>
      <artifactId>spark-tags_${scala.binary.version}</artifactId>
      <type>test-jar</type>
      <scope>test</scope>
      <version>${spark.version}</version>
    </dependency>

  </dependencies>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.scalatest</groupId>
          <artifactId>scalatest-maven-plugin</artifactId>
          <version>2.2.0</version>
          <configuration>
            <!--
              The plugin forks a JVM and resolves `java` from PATH, which can be an older
              JDK than the one Maven compiled with (Java 17 classes then fail to load with
              UnsupportedClassVersionError). Pin the fork to Maven's own JVM.
              Spark 4 on Java 17 also needs the JDK internals that Spark's own build opens.
            -->
            <jvm>${java.home}/bin/java</jvm>
            <argLine>
              --add-opens=java.base/java.lang=ALL-UNNAMED
              --add-opens=java.base/java.lang.invoke=ALL-UNNAMED
              --add-opens=java.base/java.lang.reflect=ALL-UNNAMED
              --add-opens=java.base/java.io=ALL-UNNAMED
              --add-opens=java.base/java.net=ALL-UNNAMED
              --add-opens=java.base/java.nio=ALL-UNNAMED
              --add-opens=java.base/java.util=ALL-UNNAMED
              --add-opens=java.base/java.util.concurrent=ALL-UNNAMED
              --add-opens=java.base/java.util.concurrent.atomic=ALL-UNNAMED
              --add-opens=java.base/sun.nio.ch=ALL-UNNAMED
              --add-opens=java.base/sun.nio.cs=ALL-UNNAMED
              --add-opens=java.base/sun.security.action=ALL-UNNAMED
              --add-opens=java.base/sun.util.calendar=ALL-UNNAMED
            </argLine>
          </configuration>
          <executions>
            <execution>
              <id>test</id>
              <goals>
                <goal>test</goal>
              </goals>
            </execution>
          </executions>
        </plugin>
        <plugin>
          <groupId>net.alchim31.maven</groupId>
          <artifactId>scala-maven-plugin</artifactId>
          <version>4.9.10</version>
          <executions>
            <execution>
              <id>compile</id>
              <goals>
                <goal>compile</goal>
                <goal>add-source</goal>
                <goal>doc-jar</goal>
              </goals>
              <phase>compile</phase>
            </execution>
            <execution>
              <id>test-compile</id>
              <goals>
                <goal>testCompile</goal>
              </goals>
              <phase>test-compile</phase>
            </execution>
            <execution>
              <phase>process-resources</phase>
              <goals>
                <goal>compile</goal>
              </goals>
            </execution>
          </executions>
          <configuration>
            <scalaVersion>${scala.version}</scalaVersion>
            <args>
              <arg>-nobootcp</arg>
              <arg>-release:${java.version}</arg>
            </args>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.15.0</version>
          <executions>
            <execution>
              <phase>compile</phase>
              <goals>
                <goal>compile</goal>
              </goals>
            </execution>
          </executions>
          <configuration>
            <release>${java.version}</release>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-shade-plugin</artifactId>
          <version>3.6.0</version>
          <executions>
            <execution>
              <phase>package</phase>
              <goals>
                <goal>shade</goal>
              </goals>
              <configuration>
                <artifactSet>
                  <includes>
                    <include>software.amazon.awssdk:*:*</include>
                    <include>software.amazon.eventstream:eventstream:*</include>
                    <include>software.amazon.kinesis:amazon-kinesis-producer:*</include>
                    <include>org.reactivestreams:reactive-streams:*</include>
                    <include>io.netty:netty-*:*</include>
                    <!--
                      ApacheHttpClient (pinned in KinesisReader/STSCredentials) needs these at
                      runtime. Without them the client fails with NoClassDefFoundError on
                      org.apache.http.protocol.HttpContext; relying on Spark/Hadoop's own copy
                      would make the connector's HTTP stack depend on the host classpath.
                    -->
                    <include>org.apache.httpcomponents:httpclient:*</include>
                    <include>org.apache.httpcomponents:httpcore:*</include>
                    <include>com.fasterxml.jackson.dataformat:*:*</include>
                    <include>com.google.protobuf:*:*</include>
                  </includes>
                </artifactSet>
                <relocations>
                  <relocation>
                    <pattern>com.fasterxml.jackson.dataformat</pattern>
                    <shadedPattern>org.apache.spark.sql.kinesis.shaded.fasterxml.jackson.dataformat</shadedPattern>
                    <includes>
                      <include>com.fasterxml.jackson.dataformat.**</include>
                    </includes>
                  </relocation>
                  <relocation>
                    <pattern>software.amazon.awssdk</pattern>
                    <shadedPattern>org.apache.spark.sql.kinesis.shaded.software.amazon.awssdk</shadedPattern>
                    <includes>
                      <include>software.amazon.awssdk.**</include>
                    </includes>
                  </relocation>
                  <relocation>
                    <pattern>software.amazon.eventstream</pattern>
                    <shadedPattern>org.apache.spark.sql.kinesis.shaded.software.amazon.eventstream</shadedPattern>
                    <includes>
                      <include>software.amazon.eventstream.**</include>
                    </includes>
                  </relocation>
                  <relocation>
                    <pattern>software.amazon.kinesis</pattern>
                    <shadedPattern>org.apache.spark.sql.kinesis.shaded.software.amazon.kinesis</shadedPattern>
                    <includes>
                      <include>software.amazon.kinesis.**</include>
                    </includes>
                  </relocation>
                  <relocation>
                    <pattern>org.reactivestreams</pattern>
                    <shadedPattern>org.apache.spark.sql.kinesis.shaded.org.reactivestreams</shadedPattern>
                  </relocation>
                  <!-- Relocated so the bundled copy cannot clash with Hadoop's httpclient. -->
                  <relocation>
                    <pattern>org.apache.http</pattern>
                    <shadedPattern>org.apache.spark.sql.kinesis.shaded.org.apache.http</shadedPattern>
                    <includes>
                      <include>org.apache.http.**</include>
                    </includes>
                  </relocation>
                  <relocation>
                    <pattern>io.netty</pattern>
                    <shadedPattern>org.apache.spark.sql.kinesis.shaded.io.netty</shadedPattern>
                  </relocation>
                  <relocation>
                    <pattern>com.google.protobuf</pattern>
                    <shadedPattern>org.apache.spark.sql.kinesis.shaded.google.protobuf</shadedPattern>
                    <includes>
                      <include>com.google.protobuf.**</include>
                    </includes>
                  </relocation>
                </relocations>
                <filters>
                  <filter>
                    <artifact>*:*</artifact>
                    <excludes>
                      <exclude>META-INF/LICENSE*</exclude>
                      <exclude>META-INF/NOTICE*</exclude>
                      <exclude>META-INF/DEPENDENCIES</exclude>
                      <exclude>META-INF/maven/**</exclude>
                      <exclude>META-INF/*.xml</exclude>
                      <exclude>META-INF/*.SF</exclude>
                      <exclude>META-INF/*.DSA</exclude>
                      <exclude>META-INF/*.RSA</exclude>
                      <exclude>models/*</exclude>
                      <exclude>.gitkeep</exclude>
                    </excludes>
                  </filter>
                </filters>
                <transformers>
                  <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer" />
                  <transformer implementation="org.apache.maven.plugins.shade.resource.DontIncludeResourceTransformer">
                    <resource>log4j.properties</resource>
                  </transformer>
                  <transformer implementation="org.apache.maven.plugins.shade.resource.ApacheLicenseResourceTransformer" />
                  <transformer implementation="org.apache.maven.plugins.shade.resource.ApacheNoticeResourceTransformer" />
                </transformers>
              </configuration>
            </execution>
          </executions>
        </plugin>
      </plugins>
    </pluginManagement>
    <!-- Enable surefire and scalatest in all children, in one place: -->
    <plugins>
      <plugin>
        <groupId>net.alchim31.maven</groupId>
        <artifactId>scala-maven-plugin</artifactId>
        <version>4.9.10</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <version>3.6.0</version>
      </plugin>
      <plugin>
        <groupId>org.scalatest</groupId>
        <artifactId>scalatest-maven-plugin</artifactId>
        <version>2.2.0</version>
      </plugin>
    </plugins>

  <outputDirectory>target/scala-${scala.binary.version}/classes</outputDirectory>
  <testOutputDirectory>target/scala-${scala.binary.version}/test-classes</testOutputDirectory>
  </build>

  <profiles>
    <profile>
      <id>release</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-source-plugin</artifactId>
            <version>3.4.0</version>
            <executions>
              <execution>
                <id>attach-sources</id>
                <goals>
                  <goal>jar-no-fork</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-gpg-plugin</artifactId>
            <version>3.2.8</version>
            <executions>
              <execution>
                <id>sign-artifacts</id>
                <phase>verify</phase>
                <goals>
                  <goal>sign</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-javadoc-plugin</artifactId>
            <version>3.12.0</version>
            <executions>
              <execution>
                <id>create-javadoc-jar</id>
                <goals>
                  <goal>jar</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-release-plugin</artifactId>
            <version>3.3.1</version>
            <configuration>
              <autoVersionSubmodules>true</autoVersionSubmodules>
            </configuration>
          </plugin>

          <plugin>
            <groupId>org.sonatype.plugins</groupId>
            <artifactId>nexus-staging-maven-plugin</artifactId>
            <version>1.6.7</version>
            <extensions>true</extensions>
            <configuration>
              <serverId>ossrh</serverId>
              <nexusUrl>https://s01.oss.sonatype.org/</nexusUrl>
              <autoReleaseAfterClose>true</autoReleaseAfterClose>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>

  <distributionManagement>
    <snapshotRepository>
      <id>ossrh</id>
      <url>https://s01.oss.sonatype.org/content/repositories/snapshots</url>
    </snapshotRepository>
    <repository>
      <id>ossrh</id>
      <url>https://s01.oss.sonatype.org/service/local/staging/deploy/maven2/</url>
    </repository>
  </distributionManagement>

  <reporting>
    <plugins>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>cobertura-maven-plugin</artifactId>
        <version>2.7</version>
      </plugin>
    </plugins>
  </reporting>
</project>
